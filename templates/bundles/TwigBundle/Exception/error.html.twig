{% extends 'base.html.twig' %}

{% block title %}Erreur {{ status_code|default(500) }}{% endblock %}

{% block content %}
    <div class="max-w-3xl mx-auto bg-white rounded shadow p-8 mt-12 text-center">
        <h1 class="text-4xl font-bold mb-4">Erreur {{ status_code|default(500) }} — {{ status_text|default('Erreur') }}</h1>
        <p class="text-gray-700 mb-6">Désolé, une erreur est survenue. Si le problème persiste, contactez l'administrateur.</p>
        {% if exception is defined and exception.message is defined and app.debug %}
            <pre class="text-left bg-gray-100 p-4 rounded overflow-auto">{{ exception.message }}</pre>
        {% endif %}
        <a href="{{ path('home') }}" class="inline-block mt-6 bg-blue-600 text-white px-4 py-2 rounded">Retour à l'accueil</a>
    </div>
{% endblock %}
